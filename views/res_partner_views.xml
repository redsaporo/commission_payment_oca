<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record model="ir.ui.view" id="view_partner_form_commission_payments">
        <field name="name">res.partner.form - Commission Payments</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="priority">30</field>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_commission_payments"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-money"
                    invisible="commission_payment_count == 0">
                    <div class="o_stat_info">
                        <field name="commission_payment_count" class="o_stat_value" />
                        <span class="o_stat_text">Payments</span>
                    </div>
                </button>
            </xpath>
        </field>
    </record>
</odoo>
